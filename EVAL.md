# Winnie — 技术评估（活文档）

> 本文件为技术方案可行性的**唯一迭代记录**。每次重新评估时直接改此文件，并在文末「变更记录」追加一条。

## 当前结论（2026-09-06）

**整体可行。** 约 2 个月内可完成可演示 MVP（阶段一 + 二为主，阶段三做简化版）。完整冲突解决与阶段四压测属加分项，不宜阻塞演示。

| 模块 | 可行性 | 说明 |
|------|--------|------|
| FastAPI + OpenAI 兼容接口 | 高 | 协议成熟，流式 SSE 有现成模式，1–2 周可出基线 |
| LLM 接入（GPT-4 / Claude） | 高 | 官方 SDK / OpenAI 兼容客户端即可，注意密钥与限流 |
| 向量库 + 基础 RAG | 高 | 对话检索增强是成熟路径；MVP 用托管向量库更快 |
| Redis 异步写入 | 中高 | 队列本身简单；与「提炼/去重」耦合后要设计幂等与失败重试 |
| 记忆提炼 / 去重 / 冲突解决 | 中 | 无统一最佳实践，依赖提示词与启发式规则，是体验差异化与主要工期风险 |
| 用户隔离与权限 | 中高 | 技术不难；需尽早定 `user_id` / namespace，避免后期改向量元数据 |

### 建议定稿

1. **编排**：MVP 优先 LangChain；若后续以索引/检索为中心再评估 LlamaIndex。
2. **向量库**：演示期优先 Pinecone（或同类托管）；自建 Milvus 延后。
3. **队列**：Redis + 轻量 worker（RQ / Celery / arq）。
4. **隔离**：第二阶段起按 `user_id`（及可选 `session_id`）隔离；第四阶段补鉴权。
5. **冲突**：MVP 用「相似度去重 + 新覆盖旧」；完整仲裁后置。

### 主要风险

- 记忆噪声：原文直存易脏检索 → 提炼后再入库。
- 异步一致性：刚写入尚未可检索 → 同步短缓存或演示规避。
- 范围膨胀：工具调用 / 多 Agent / 完整冲突引擎挤占 RAG 质量。

---

## 对记忆架构的评审意见

方案把「记忆驱动」落在 RAG 上，作为 2 个月 MVP 的技术路径是合理且可交付的，但不宜把 RAG 理解成 AI 或本产品的终局形态。权重内知识难更新、难审计、难按用户隔离；把可变事实放在模型外，正是个性化智能体成立的前提。更稳妥的预期是：检索增强会与 Agent 工具调用、结构化记忆（乃至图谱）长期并存——Winnie 现阶段用个性化记忆检索证明价值即可，架构上为后续扩展留口，而不是押注「只有更大的模型」或「只有 RAG」。

可行性上真正要守住的，是存储与可介入性，而不是向量库品牌。每条记忆必须同时具备**可编辑的文本真相源**（提炼后的自然语言 + JSON 元数据）和**由该文本算出的 embedding**。后者在 Pinecone/Milvus 内多以浮点紧凑/类二进制形态存放，只服务近邻检索，人不可读，也绝不能当作唯一知识本体。MVP 推荐向量库挂 `text + embedding + metadata`；若排查频繁，可并行用 Postgres/SQLite 存完整记忆表、向量库只做索引。若只落二进制向量、不留可改文本，后续去重、冲突解决和人工纠错都会失去抓手——这比选错托管/自建更伤可行性。

因此第二阶段起就应把「人能检查知识库」算进范围，而不是留到上线前补丁。最低标准：按 `user_id` 列出/查看/修改/删除记忆，改文本后强制重算 embedding；debug 对话附带本次命中的 memory id，便于验证检索是否在帮倒忙；支持按用户导出 JSON/CSV。控制台可后置，但编辑入口应走自有 API，以便审计与重嵌入。验收时可复用：能否列全某用户记忆、改一条后下次对话是否生效、删一条后是否不再被命中——这三项不过，记忆子系统只能算半成品。

---

## 变更记录

| 日期 | 变更摘要 |
|------|----------|
| 2026-09-06 | 初版：从 DESIGN 迁入可行性结论；补充记忆架构相关判断 |
| 2026-09-06 | 将 RAG/存储/介入相关问答改写为连贯评审意见，融入评估正文 |
