# Winnie — 技术评估（活文档）

> 本文件为技术方案可行性的**唯一迭代记录**。每次重新评估时直接改此文件，并在文末「变更记录」追加一条。

## 当前结论（2026-09-06）

**整体仍可行，但范围已上修；栈已按开源与可维护性定稿。** 在原「API + RAG」之上增加管理 UI、Git 文档版控与自动分类打标后，2 个月仍可做出可演示 MVP，前提是：阶段二交付「能编、能追历史、能重纳入、能按类筛选」的闭环。技术栈默认：**LangGraph + Qdrant + Vite/React + Redis/arq + LiteLLM**（详见 DESIGN 选型原则）。编排以通用 Agent 为准，记忆/RAG 为图中能力而非唯一主框架。完整冲突解决、深层级分类树、精美 UI 仍应后置。

| 模块 | 可行性 | 说明 |
|------|--------|------|
| FastAPI + OpenAI 兼容接口 | 高 | 不变；仍是最快基线 |
| LLM 接入（LiteLLM 多模型） | 高 | 开源路由；兼用于提炼与自动分类 |
| Git 文档知识库 | 高 | 文本版控成熟；按用户仓/目录隔离即可 |
| LangGraph Agent + Qdrant 检索 | 高 | 通用 Agent 主路径；记忆作检索节点/工具；索引对齐 Git HEAD |
| 管理 UI（Vite + React） | 中高 | 薄 UI + 管理 API 可演示；勿精修抢工期 |
| 自动分类与打标 | 中高 | 主分类表已定；低置信进 `unsorted` |
| Redis + arq | 中高 | 重纳入、分类、嵌入口队；需幂等 |
| 记忆去重 / 冲突解决 | 中 | MVP 保持「相似去重 + 新覆盖旧」 |
| 用户隔离与权限 | 中高 | Git 路径与 Qdrant payload 的 `user_id` 需与 UI 鉴权一并设计 |

### 建议定稿

1. **编排**：LangGraph（通用 Agent）；不默认 LlamaIndex 作主框架（其可做 Agent，但偏 RAG；避免双框架）。
2. **向量库**：Qdrant（开源、单机简单；不用 Pinecone 作默认）。
3. **真相源**：用户知识以 Git 中 Markdown 为准；向量库为派生索引。
4. **队列**：Redis + arq；UI「重新纳入」显式任务并显示状态。
5. **前端**：Vite + React。
6. **模型**：LiteLLM / OpenAI 兼容，配置切换。
7. **隔离**：`user_id` → Git 仓（或前缀）+ Qdrant payload 过滤。
8. **冲突**：MVP「相似度去重 + 新覆盖旧」。
9. **分类**：DESIGN 十大主分类；单主类 + 多标签。

### 主要风险

- **范围膨胀**：UI + Git + 分类若与冲突引擎、多 Agent 并行，演示质量会掉——阶段二砍范围优先保闭环。
- **Git 与索引漂移**：只改 Git 未重纳入、或只改向量未 commit，会导致「UI 看见的」与「模型用到的」不一致 → 写路径必须 commit 后入队索引。
- **分类噪声**：自动打标不稳会污染筛选与检索过滤 → 保留 `unsorted` + 人工改类，检索默认可降权未整理条目。
- **异步一致性 / 记忆噪声**：原有风险仍在；提炼后再入库，演示脚本避开「刚写完立刻问」。

---

## 对记忆架构的评审意见

方案把「记忆驱动」落在 RAG 上，作为 2 个月 MVP 的技术路径合理可交付，但不宜把 RAG 当成 AI 或本产品的终局。可变事实放在模型外，才能更新、审计、按用户隔离；更稳妥的形态是检索增强与工具调用、结构化记忆长期并存。Winnie 现阶段用个性化记忆证明价值即可。

在已补充的产品思路下，可行性关键从「会不会做向量检索」转为「真相源是否可版本化、可人工介入」。以 **Git 管理文档知识库** 直接回应了变更追踪需求：每次 UI 编辑或自动提炼的落盘都是 commit，历史、diff、回滚都有现成工具链，也强制团队把**可编辑文本**当作真相、把 embedding 当作派生索引（库内浮点紧凑存储只服务近邻检索）。管理 UI 负责编辑文档并「重新纳入」，使纠错闭环不依赖调向量库控制台；若出现只改索引不改 Git，或回滚后未重纳入，记忆子系统应判为不合格。

自动分类与打标应服务于管理与过滤，而不是另造一套复杂本体。DESIGN 中的主分类（身份、偏好、人物、项目、领域、流程、红线、资源、事件、待整理）按「记忆用途」划分，比按「生活领域」零散枚举更稳；标签承担人物名、项目名、时效与敏感等交叉维。自动流程选一个主类 + 多标签，低置信进 `unsorted`，避免错误类别进入检索过滤白名单。这样 Git、UI、分类与向量索引才能咬成同一条故事：人在 UI 改文档 → Git 留史 → 重纳入更新索引 → 对话按类检索可解释。

---

## 变更记录

| 日期 | 变更摘要 |
|------|----------|
| 2026-09-06 | 初版：从 DESIGN 迁入可行性结论；补充记忆架构相关判断 |
| 2026-09-06 | 将 RAG/存储/介入相关问答改写为连贯评审意见 |
| 2026-09-06 | 纳入 UI、Git 版控、自动分类打标；上修范围结论与风险；评审意见与 DESIGN 分类表对齐 |
| 2026-09-06 | 栈定稿：LlamaIndex、Qdrant、Vite/React、arq、LiteLLM；与 DESIGN 选型原则对齐 |
| 2026-09-06 | 按「通用 AI」目标修正编排：主框架改为 LangGraph；澄清 LlamaIndex 可做 Agent 但不作默认 |
