# 基础密钥（各环境可共用同一兼容网关，用不同 model 名区分）
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://api.openai.com/v1
#
# 按环境绑定的默认模型（可按需改）
MODEL_DEV=gpt-4o-mini
MODEL_TEST=gpt-4o-mini
MODEL_PROD=gpt-4o
#
# 启动默认环境：dev | test | prod
APP_ENV=dev
