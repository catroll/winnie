# 24 · langchain-loader

**Document Loader**：把文件变成 `Document`。

```text
PDF / Text / CSV / Web → Document
```

## 学习目标

1. 认识 TextLoader、PDF、Web、CSV 等 Loader 的统一产出物。
2. 读懂 `page_content` 与 `metadata`（如 source）。
3. 为本仓库后续 Splitter / Embedding / RAG 准备原始文档。

```bash
cd playground/24langchain-loader && uv sync && uv run python main.py
```
