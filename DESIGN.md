# Winnie — 系统设计

## 概述

Winnie 是具备长期记忆能力的通用 AI 智能体，通过 OpenAI 兼容接口对外服务。核心采用「记忆驱动」架构：交互与文档经语义提炼后进入知识库，再经向量检索增强生成，支撑个性化、可审计的长期记忆。

目标：约 2 个月内完成可演示的 MVP。

## 设计原则

1. **无厂商锁定**：能力通过开放接口编排；模型、向量库等可替换，切换模型不得迫使改业务语义或知识格式。
2. **文档即知识**：全部长期知识以 Markdown 为载体；索引可重建，文档不可被向量库取代。
3. **来源可辨**：每条知识标明出处（用户文档 / 某次对话沉淀 / UI 编辑等），禁止来源不明的静默写入。
4. **变更可溯**：知识的每次增删改均可追踪（谁、何时、因何、相对前版 Diff）。
5. **交互必沉淀**：每一轮有效交互都要提炼经验并写入知识库（无新信息则显式记「无增量」），禁止只聊不记。
6. **人可介入**：知识必须可读、可改、可回滚；自动分类与检索服从人工修订。
7. **检索可解释**：回答所依据的知识条目应可回溯；默认可关闭的调试视图展示命中项。
8. **用户边界清晰**：知识按用户（或工作区）隔离，检索与管理不得串读。

## 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 语言 | Python | 主开发语言 |
| 服务层 | FastAPI | OpenAI 兼容 HTTP API + 知识库管理 API |
| 管理 UI | Web 前端（框架待定） | 对话调试 + 知识库编辑 / 重纳入 |
| 智能体编排 | LangChain / LlamaIndex | 对话与 RAG 流程编排 |
| 文档真相源 | Git 仓库（按用户隔离） | 知识文档版本管理与变更追踪 |
| 向量记忆库 | Pinecone / Milvus | 语义检索索引（非唯一真相） |
| 异步任务 | Redis | 提炼、分类打标、重索引等异步队列 |
| LLM | GPT-4 / Claude API | 推理、提炼、自动分类打标 |

## 基本目录结构

```text
winnie/
├── apps/api/       # FastAPI：Chat + 知识库 API
├── apps/web/       # 管理 UI
├── src/agent/      # 编排与 RAG
├── src/memory/     # 提炼、分类、Git、向量索引
├── src/worker/     # Redis 异步任务
├── data/kb/        # 运行时用户 Git 知识库
└── tests/
```

## 核心架构

```
管理 UI ──► 编辑文档 / 触发重纳入
              │
              ▼
         Git 知识库（文本真相源 + 版本历史）
              │ commit / 同步写入
              ▼
FastAPI 网关
  │
  ├─► Chat Completions（OpenAI 兼容）
  │     └─► 检索向量索引 → LLM 生成
  │
  └─► 记忆流水线（Redis 异步）
        提炼 → 自动分类/打标 → 去重 → 写入 Git + 更新向量索引
```

要点：

- **双轨真相**：人可编辑的 Markdown/文本落在 **Git**；**向量库**只存检索用 embedding 与元数据副本。
- **对外契约**：标准 Chat Completion 接口，便于现有客户端接入。
- **记忆闭环**：对话/文档 → 语义提炼 → 分类打标 → Git 固化 → 向量化 → RAG。
- **写入异步化**：重计算走队列，避免阻塞对话延迟；UI「重新纳入」显式触发全量或增量重索引。

## 知识库与管理 UI

### 管理界面

提供 Web UI，至少覆盖：

- 按用户浏览知识库文档与条目
- 在线编辑文档内容并保存（底层写 Git commit）
- 手动调整分类 / 标签
- 「重新纳入知识库」：对选定文档或全部文档重新跑提炼（可选）与 embedding 索引
- 查看变更历史（基于 Git log / diff）
- （可选，演示用）对话 debug：展示本轮命中的知识条目

### Git 版本管理

- 每个用户（或每个工作区）对应独立 Git 仓库或独立目录树 + 统一远端策略；文档以 Markdown 等纯文本为主。
- 所有 UI/API 写操作通过 commit 落盘，commit message 记录来源（`ui-edit` / `auto-extract` / `reindex` 等）。
- 支持查看历史、对比 diff、回滚到指定版本后再触发重纳入。
- 向量索引与 Git HEAD 对齐：commit 成功后入队更新索引；回滚同样触发索引重建或增量修补。

### 自动分类与标签

入库（对话提炼或文档纳入）时由 LLM + 规则完成：

1. 选择**一个主分类**（见下表）
2. 附加**若干标签**（自由词 + 推荐词，多选）
3. 结果写入文档 front matter / 侧车元数据，并同步到向量 metadata，便于过滤检索与 UI 筛选

人工可随时改分类与标签；修改后可仅更新元数据索引，或按需重算 embedding。

## 知识分类体系（初始）

主分类彼此尽量互斥，便于自动归类；标签用于交叉维度（人物名、项目名、技术栈等）。未置信时归入 `unsorted`，由人工或定期任务再分。

| 代码 | 名称 | 收纳内容 | 示例 |
|------|------|----------|------|
| `identity` | 身份档案 | 用户自称、称呼偏好、基本背景 | 「叫我 Cat」「时区 UTC+8」 |
| `preferences` | 偏好习惯 | 沟通风格、审美、工作/工程习惯、工具偏好 | 「PR 要小步提交」「回答先给结论」 |
| `people` | 人物关系 | 家人、同事、朋友及关系备注 | 「伴侣不吃香菜」「搭档负责后端」 |
| `projects` | 项目目标 | 进行中的事、里程碑、责任范围 | 「Winnie 两月 MVP」「本周只做网关」 |
| `domain` | 领域知识 | 用户教导的事实、领域要点、可复用说明 | 「公司用 Milvus」「内部术语 X 指 Y」 |
| `procedures` | 流程方法 | SOP、操作步骤、排查路径 | 「发版先跑 lint」「故障先看 Redis」 |
| `constraints` | 规则红线 | 必须/禁止、合规与安全约束 | 「勿外传客户名」「默认中文回复」 |
| `resources` | 环境资源 | 设备、账号别名、路径、环境与依赖 | 「主力机是 Linux」「API 走代理」 |
| `episodes` | 事件决策 | 有时间锚点的重要事件与已做决定 | 「2026-09 选定 Git 管知识库」 |
| `unsorted` | 待整理 | 自动分类低置信或无法归入上列 | 临时摘录 |

**推荐标签维度**（非穷举，可自动建议 + 用户增删）：

- 实体：`person:*` / `project:*` / `tool:*`
- 时效：`stable`（长期有效）/ `temporary`（短期）/ `stale-candidate`（可能过期）
- 来源：`from-chat` / `from-doc` / `from-ui`
- 敏感：`sensitive`（UI 可折叠，检索可降权或需确认）

后续若某主分类长期过载，再拆子类；MVP 不提前做深层级树。

## 开发路线

### 第一阶段：MVP（无状态对话）

- 搭建 FastAPI 网关与 OpenAI 兼容 Chat Completion
- 对接基础 LLM，实现无状态对话

### 第二阶段：记忆核心（RAG + 文档真相源）

- Git 知识库落地与按用户隔离
- 向量库集成；文档/记忆索引与语义检索
- RAG 对话；管理 API（列表/编辑/重纳入）
- 自动分类打标（按初始分类表）
- 管理 UI 初版（编辑文档、看历史、触发重纳入）

### 第三阶段：智能管理

- Redis 异步队列强化提炼、去重与冲突解决
- 分类置信度与 `unsorted` 清理任务
- Git 回滚与索引一致性修补

### 第四阶段：上线就绪

- 用户隔离与权限管理（UI + API）
- 性能压测与接口稳定性优化

## 里程碑

| 阶段 | 交付物 | 大致周期（合计约 2 个月） |
|------|--------|---------------------------|
| 一 | 可调用的兼容对话 API | 优先完成，支撑演示基线 |
| 二 | Git 知识库 + RAG + 管理 UI 初版 | MVP 核心能力 |
| 三 | 异步治理与分类清理 | 质量与可扩展性 |
| 四 | 多用户权限与稳定性 | 演示 / 试用就绪 |

## 可行性评估

技术方案可行性的**迭代评估**见 [EVAL.md](./EVAL.md)（每次重新评估只改该文件）。

## 记忆架构要点（摘要）

评审全文见 [EVAL.md](./EVAL.md)。要点：以 Git 文档为可版本化真相源、向量为索引；提供 UI 编辑与重纳入；入库自动主分类 + 标签，主分类见上文「知识分类体系」。
