# Winnie — 系统设计

## 概述

Winnie 是具备长期记忆能力的通用 AI 智能体，通过 OpenAI 兼容接口对外服务。核心采用「记忆驱动」架构：将交互信息经语义提炼后写入向量数据库，支撑个性化、可检索的长期记忆。

目标：约 2 个月内完成可演示的 MVP。

## 技术栈

| 层级 | 选型 | 说明 |
|------|------|------|
| 语言 | Python | 主开发语言 |
| 服务层 | FastAPI | OpenAI 兼容 HTTP API |
| 智能体编排 | LangChain / LlamaIndex | 对话与 RAG 流程编排 |
| 向量记忆库 | Pinecone / Milvus | 语义存储与检索 |
| 异步任务 | Redis | 记忆写入、提炼等异步队列 |
| LLM | GPT-4 / Claude API | 底层推理与语义提炼 |

## 核心架构

```
客户端
  │  OpenAI Chat Completions 兼容请求
  ▼
FastAPI 网关
  │
  ├─► LLM（无状态 / RAG 增强生成）
  │
  └─► 记忆子系统
        ├─ 语义检索（向量库）
        └─ 异步写入队列（Redis）
              └─ 提炼 / 去重 / 冲突解决 → 向量库
```

要点：

- **对外契约**：标准 Chat Completion 接口，便于现有客户端接入。
- **记忆闭环**：对话 → 语义提炼 → 向量化存储 → 检索增强生成。
- **写入异步化**：重计算（提炼、去重、冲突处理）走队列，避免阻塞对话延迟。

## 开发路线

### 第一阶段：MVP（无状态对话）

- 搭建 FastAPI 网关
- 封装 OpenAI 兼容 Chat Completion 接口
- 对接基础 LLM，实现无状态对话

### 第二阶段：记忆核心（RAG）

- 集成向量数据库
- 对话历史向量化存储与语义检索
- 构建检索增强生成（RAG）流程

### 第三阶段：智能管理

- 引入基于 Redis 的异步任务队列
- 记忆自动提炼、去重与冲突解决

### 第四阶段：上线就绪

- 用户隔离与权限管理
- 性能压测与接口稳定性优化

## 里程碑

| 阶段 | 交付物 | 大致周期（合计约 2 个月） |
|------|--------|---------------------------|
| 一 | 可调用的兼容对话 API | 优先完成，支撑演示基线 |
| 二 | 带语义记忆的 RAG 对话 | MVP 核心能力 |
| 三 | 异步记忆治理流水线 | 质量与可扩展性 |
| 四 | 多用户与稳定性 | 演示 / 试用就绪 |

## 可行性评估

技术方案可行性的**迭代评估**见 [EVAL.md](./EVAL.md)（每次重新评估只改该文件）。

## 记忆架构要点（摘要）

评审全文见 [EVAL.md](./EVAL.md)。要点：RAG 是 MVP 合理路径而非终局；记忆以可编辑文本为真相源、embedding 仅为检索索引；第二阶段起即具备列表/改删/debug 命中/导出等人工介入面。
